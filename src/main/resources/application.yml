spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trip?serverTimezone=Asia/Seoul
    username: root
    password: qls515
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type : trace

server:
  port: 8070